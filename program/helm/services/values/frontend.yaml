image:
  repository: tonynm/frontend
  tag: latest
  pullPolicy: Always

service:
  fullname: frontend-service
  name: frontend
  port: 80

env:
  - name: IDP_API_URL
    value: "http://identity.local"
  - name: API_URL
    value: "http://gateway.local"
  - name: CLIENT_ID
    value: "locus-frontend-client"

ingress:
  enabled: true
  name: frontend-ingress
  host: frontend.local
  path: /
